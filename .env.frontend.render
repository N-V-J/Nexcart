# Render Frontend Deployment Configuration
# Use this file when deploying navinvj/nexcart-frontend Docker image on Render

# API Configuration
# Replace with your actual backend URL on Render
VITE_API_URL=https://nexcart-backend.onrender.com/api

# Stripe Configuration (Optional)
VITE_STRIPE_PUBLIC_KEY=your-stripe-public-key

# App Configuration
VITE_APP_NAME=NexCart
VITE_APP_DESCRIPTION=Modern E-Commerce Platform

# Environment
NODE_ENV=production

# Note: The Docker image is pre-built with production settings
# These environment variables can override the build-time values if needed
